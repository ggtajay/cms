import React from 'react'
import ParentPortal from '../../components/ParentPortal'

const ParentResults = () => (
  <ParentPortal
    title="Child Results"
    subtitle="This page is prepared for exam and result publishing once that module is connected."
    mode="results"
  />
)

export default ParentResults
